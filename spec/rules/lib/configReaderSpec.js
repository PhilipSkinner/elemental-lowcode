const
	jasmine 		= require('jasmine'),
	sinon 			= require('sinon'),
	configReader 	= require('../../../rules/lib/configReader');

describe("A config reader", () => {
	it("empty test", () => {

	});
});
